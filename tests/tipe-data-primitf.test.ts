describe('Data Type', function() {
    it('should must declare', function() {
        let name:string = "Enricho Alkalas"
        let balance:number = 10000
        let isVip:boolean = true
        
        // console.info(name)
        // console.info(balance)
        // console.info(isVip)
    })
})